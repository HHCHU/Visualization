// 343540
let canvasWidth;
let canvasHeight;
//DOM
const p3MainCC = document.getElementById("p4MainCC");

canvasWidth = p4MainCC.getBoundingClientRect().width;
canvasHeight = p4MainCC.getBoundingClientRect().height;
