// 343540
let canvasWidth;
let canvasHeight;
//DOM
const p8MainCC = document.getElementById("p8MainCC");

canvasWidth = p8MainCC.getBoundingClientRect().width;
canvasHeight = p8MainCC.getBoundingClientRect().height;
