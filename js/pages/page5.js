// 343540
let canvasWidth;
let canvasHeight;
//DOM
const p5MainCC = document.getElementById("p5MainCC");

canvasWidth = p5MainCC.getBoundingClientRect().width;
canvasHeight = p5MainCC.getBoundingClientRect().height;
